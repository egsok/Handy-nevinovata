name: "Release"

on:
  workflow_dispatch:
  push:
    tags: ["v*-*"]

jobs:
  create-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      release-id: ${{ steps.create-release.outputs.result }}
      version: ${{ steps.get-version.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Get version from tauri.conf.json
        id: get-version
        shell: bash
        run: |
          VERSION=$(grep -o '"version": "[^"]*"' src-tauri/tauri.conf.json | cut -d'"' -f4)
          echo "Application version from tauri.conf.json: $VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create Draft Release
        id: create-release
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('node:fs');
            const version = '${{ steps.get-version.outputs.version }}';
            const body = fs.readFileSync(`RELEASE-NOTES-v${version}.md`, 'utf8');
            const { data } = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: `v${version}`,
              target_commitish: context.sha,
              name: `Клава не виновата v${version}`,
              body,
              draft: true,
              prerelease: false,
              generate_release_notes: false
            })
            return data.id

  publish-tauri:
    permissions:
      contents: write
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-26" # ARM macs (Apple Silicon) — experimental, untested by maintainer
            args: "--target aarch64-apple-darwin"
            target: "aarch64-apple-darwin"
          - platform: "macos-latest" # Intel macs — experimental, untested by maintainer
            args: "--target x86_64-apple-darwin"
            target: "x86_64-apple-darwin"
          - platform: "ubuntu-22.04" # .deb
            args: "--bundles deb"
            target: "x86_64-unknown-linux-gnu"
          - platform: "ubuntu-24.04" # AppImage + RPM
            args: "--bundles appimage,rpm"
            target: "x86_64-unknown-linux-gnu"
          - platform: "windows-latest"
            args: ""
            target: "x86_64-pc-windows-msvc"

    uses: ./.github/workflows/build.yml
    with:
      platform: ${{ matrix.platform }}
      target: ${{ matrix.target }}
      build-args: ${{ matrix.args }}
      sign-binaries: false
      asset-prefix: "klava-nevinovata"
      upload-artifacts: false
      release-id: ${{ needs.create-release.outputs.release-id }}
      # Releases always build from scratch — never restore the Rust cache, so a
      # stale cached native lib can never be linked into a shipped artifact.
      no-cache: true
    secrets: inherit

  publish-release:
    permissions:
      contents: write
    needs: [create-release, publish-tauri]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Prepend Download link to release notes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BODY="$(gh release view "v${{ needs.create-release.outputs.version }}" --json body -q .body --repo "${{ github.repository }}")"
          gh release edit "v${{ needs.create-release.outputs.version }}" \
            --notes "$(printf 'See [Installation instructions](https://github.com/${{ github.repository }}#download) in README.\n\n%s' "$BODY")" \
            --repo "${{ github.repository }}"

      - name: Flip draft to false
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "v${{ needs.create-release.outputs.version }}" --draft=false --repo "${{ github.repository }}"
